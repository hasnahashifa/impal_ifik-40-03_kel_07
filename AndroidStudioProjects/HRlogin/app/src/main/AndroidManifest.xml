<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hrlogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presensiManager"></activity>
        <activity android:name=".viewGaji" />
        <activity android:name=".inputGaji" />
        <activity android:name=".lembur" />
        <activity android:name=".denda" />
        <activity android:name=".claimGaji" />
        <activity android:name=".bayarGaji" />
        <activity android:name=".viewPresensi" />
        <activity android:name=".mainMenuManager" />
        <activity android:name=".mainmenuPegawai" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>